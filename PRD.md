# Product Requirements Document (PRD): Teoycodex

**สถานะ:** Draft / Discovery

**เวอร์ชัน:** 0.1

**วันที่:** 29 สิงหาคม 2026

**เจ้าของเอกสาร:** Product ทีม Teoycodex

## 1. ภาพรวม

Teoycodex เป็นผลิตภัณฑ์ที่ยังอยู่ในช่วงเก็บความต้องการเบื้องต้น ณ เวลาจัดทำเอกสารนี้ มีข้อมูลยืนยันเพียงชื่อผลิตภัณฑ์คือ **Teoycodex** และข้อความที่กล่าวถึงการกำหนด **role** แต่รายละเอียดถูกส่งมาไม่ครบถ้วน

เอกสารนี้จึงเป็น PRD แบบ Discovery: กำหนดโครงสร้างการตัดสินใจ ขอบเขต MVP สมมติฐานที่ต้องพิสูจน์ และคำถามที่ต้องตอบก่อนเริ่มพัฒนา เป้าหมายคือทำให้ทีมแปลง requirement ที่ยังไม่สมบูรณ์เป็นแผนงานที่นำไปสร้างจริงได้ โดยไม่ตีความสิ่งที่ยังไม่ยืนยันเป็นข้อเท็จจริง

## 2. สิ่งที่ยืนยันแล้ว

| หัวข้อ | รายละเอียด | สถานะ |
| --- | --- | --- |
| ชื่อผลิตภัณฑ์ | Teoycodex | ยืนยันแล้ว |
| การจัดการบทบาทผู้ใช้ | มี 2 บทบาท: `Admin` และ `User` | ยืนยันแล้ว |
| สิทธิ์ Admin | เข้าสู่ระบบและเข้าถึงได้ทุกหน้าของผลิตภัณฑ์ | ยืนยันแล้ว |
| สิทธิ์ User | เข้าถึงเฉพาะหน้าทั่วไปที่กำหนดให้ผู้ใช้ | ยืนยันแล้ว |
| ทิศทาง UI | โทนมินิมัล | ยืนยันแล้ว |
| กลุ่มผู้ใช้ | ยังไม่ระบุ | ต้องยืนยัน |
| ปัญหาที่ต้องการแก้ | ยังไม่ระบุ | ต้องยืนยัน |
| แพลตฟอร์ม | ยังไม่ระบุ | ต้องยืนยัน |
| ขอบเขต MVP | ยังไม่ระบุ | ต้องยืนยัน |

## 3. ปัญหาและโอกาส

### 3.1 Problem statement (รอการยืนยัน)

ยังไม่สามารถกำหนด problem statement ที่ถูกต้องได้ เพราะไม่มีข้อมูลว่าผู้ใช้เป้าหมายคือใคร ใช้ Teoycodex เพื่อทำงานใด และอุปสรรคในปัจจุบันคืออะไร

รูปแบบที่เจ้าของผลิตภัณฑ์ควรยืนยัน:

> ผู้ใช้กลุ่ม **[persona]** ต้องการ **[งาน/ผลลัพธ์]** เพราะปัจจุบัน **[ปัญหา]** ส่งผลให้ **[ผลกระทบที่วัดได้]**

### 3.2 Opportunity statement (รอการยืนยัน)

Teoycodex จะมีคุณค่าก็ต่อเมื่อช่วยให้ผู้ใช้บรรลุผลลัพธ์ดังกล่าวได้ง่ายขึ้น เร็วขึ้น ถูกต้องขึ้น หรือควบคุมสิทธิ์และข้อมูลได้เหมาะสมกว่าวิธีเดิม โดยต้องพิสูจน์ด้วยตัวชี้วัดในหัวข้อ 11

## 4. วิสัยทัศน์และเป้าหมายผลิตภัณฑ์

### 4.1 Product vision (รอการยืนยัน)

สร้าง Teoycodex ให้เป็นเครื่องมือ/แพลตฟอร์มที่ช่วย **[กลุ่มผู้ใช้]** จัดการ **[งานหลัก]** ได้อย่าง **[คุณลักษณะคุณค่า เช่น รวดเร็ว ปลอดภัย ตรวจสอบได้]**

### 4.2 เป้าหมาย MVP

1. ทำให้ผู้ใช้เป้าหมายสามารถทำงานหลักแบบ end-to-end ได้อย่างน้อยหนึ่งเส้นทาง
2. ทำให้ผู้ดูแลสามารถควบคุมการเข้าถึงตามบทบาทที่ตกลงกันได้
3. เก็บข้อมูลการใช้งานและ feedback เพื่อตัดสินใจรอบถัดไปด้วยหลักฐาน
4. กำหนดเกณฑ์ความสำเร็จที่วัดได้ก่อนเปิดใช้งานจริง

### 4.3 สิ่งที่ไม่ใช่เป้าหมายของ MVP

- พัฒนาฟีเจอร์ทุกกรณีใช้งานก่อนยืนยันงานหลักและผู้ใช้หลัก
- รองรับทุกบทบาทหรือโครงสร้างสิทธิ์ที่ซับซ้อนตั้งแต่เวอร์ชันแรก
- เชื่อมต่อระบบภายนอกจำนวนมากโดยยังไม่มี use case และเจ้าของ integration ที่ชัดเจน
- ปรับแต่งหน้าจอหรือ workflow ได้ไร้ขีดจำกัดก่อนพิสูจน์ product-market fit

## 5. ผู้มีส่วนได้ส่วนเสีย

| กลุ่ม | ความรับผิดชอบ/ความต้องการที่ต้องยืนยัน |
| --- | --- |
| Product owner | นิยามปัญหา ลำดับความสำคัญ และอนุมัติขอบเขต MVP |
| End user | ใช้งาน workflow หลักและให้ feedback เรื่องคุณค่า/ความง่ายในการใช้ |
| Administrator | จัดการผู้ใช้ บทบาท สิทธิ์ และการเข้าถึงข้อมูล |
| Operations/Support | รับมือเหตุขัดข้อง ตรวจสอบการใช้งาน และช่วยเหลือผู้ใช้ |
| Engineering | ประเมินความเป็นไปได้ ออกแบบสถาปัตยกรรม และพัฒนา |
| Security/Compliance | ระบุระดับข้อมูล ข้อกำหนดความปลอดภัย และนโยบายการเก็บข้อมูล |

## 6. Personas และบทบาทผู้ใช้

Teoycodex ใช้ role-based access control (RBAC) จำนวน 2 บทบาทเท่านั้นใน MVP: `Admin` และ `User`.

### 6.1 บทบาทที่กำหนด

| Role | วัตถุประสงค์ | สิทธิ์ระดับสูง | สถานะ |
| --- | --- | --- | --- |
| User | ใช้งานหน้าทั่วไปและทำงานที่ผลิตภัณฑ์อนุญาตแก่ผู้ใช้ | เข้าถึงเฉพาะหน้าทั่วไปและข้อมูลภายในขอบเขตที่กำหนด | ยืนยันแล้ว |
| Admin | ดูแลและจัดการผลิตภัณฑ์ | เข้าสู่ระบบและเข้าถึงได้ทุกหน้าของผลิตภัณฑ์ รวมถึงหน้าสำหรับผู้ดูแล | ยืนยันแล้ว |

### 6.2 หลักการออกแบบสิทธิ์

1. ใช้หลัก least privilege: ผู้ใช้ได้รับเฉพาะสิทธิ์เท่าที่จำเป็นต่อหน้าที่
2. `Admin` ต้องได้รับอนุญาตให้เข้าถึงทุก route และทุกหน้าที่ล็อกอินแล้วในผลิตภัณฑ์
3. `User` ต้องเข้าถึงได้เฉพาะ route ที่จัดเป็นหน้าทั่วไป และต้องถูกปฏิเสธจากหน้าผู้ดูแล
4. ห้ามพึ่งพาเฉพาะการซ่อนเมนูหรือปุ่มใน UI ต้องบังคับสิทธิ์ที่ backend/API ด้วย
5. เก็บ audit trail สำหรับการเปลี่ยน role การลบข้อมูล และการกระทำที่มีผลสำคัญ

### 6.3 Permission matrix

| ความสามารถ | User | Admin |
| --- | ---: | ---: |
| เข้าสู่ระบบ | ได้ | ได้ |
| เข้าถึงหน้าทั่วไป | ได้ | ได้ |
| เข้าถึงหน้าผู้ดูแล | ไม่ได้ | ได้ |
| เข้าถึงหน้าทั้งหมดในระบบ | ไม่ได้ | ได้ |
| จัดการผู้ใช้และ role | ไม่ได้ | ได้ |
| ดู audit log | ไม่ได้ | ได้ |
| ตั้งค่าระบบ | ไม่ได้ | ได้ |

## 7. ขอบเขตฟังก์ชัน MVP

เนื่องจาก domain ของ Teoycodex ยังไม่ถูกระบุ ฟังก์ชันด้านล่างแบ่งเป็นความสามารถพื้นฐานที่ต้องยืนยันความจำเป็นก่อนรับเข้าขอบเขต

### 7.1 Identity และการเข้าถึง

- ผู้ใช้สมัคร/เข้าสู่ระบบด้วยวิธีที่ธุรกิจกำหนด
- ผู้ใช้ logout และกู้คืนการเข้าถึงบัญชีได้อย่างปลอดภัย
- ระบบระบุ role ของผู้ใช้และบังคับสิทธิ์ทุกคำขอ
- ผู้ดูแลเพิ่ม ปิดใช้งาน และกำหนด role ให้ผู้ใช้ได้
- `Admin` เข้าถึงได้ทุกหน้าหลังเข้าสู่ระบบ
- `User` เข้าถึงได้เฉพาะหน้าทั่วไป และระบบต้องปฏิเสธการเข้าถึงหน้าผู้ดูแล
- ระบบแสดงเมนูและการกระทำที่เหมาะกับสิทธิ์ โดยมีข้อความกรณีไม่มีสิทธิ์เข้าถึง

**Acceptance criteria:**

- `Admin` ที่เข้าสู่ระบบแล้วเปิดทุก route ในผลิตภัณฑ์ได้สำเร็จ
- `User` ที่เข้าสู่ระบบแล้วเปิดหน้าทั่วไปได้ แต่ไม่สามารถเข้าถึงหน้าผู้ดูแลผ่าน UI หรือ API
- การเปลี่ยน role มีผลภายในเวลาที่ตกลงกัน และมีบันทึกผู้กระทำ เวลา และค่าก่อน/หลัง
- บัญชีที่ปิดใช้งานไม่สามารถสร้าง session ใหม่ได้

### 7.2 งานหลักของผลิตภัณฑ์ (Core workflow)

ก่อนพัฒนา ต้องระบุ workflow หลักหนึ่งเส้นทางในรูปแบบนี้:

1. ผู้ใช้เริ่มจาก **[จุดเริ่มต้น]**
2. ผู้ใช้ป้อนหรือเลือก **[ข้อมูลเข้า]**
3. Teoycodex ตรวจสอบและประมวลผล **[กฎธุรกิจ]**
4. ระบบสร้างหรืออัปเดต **[ข้อมูลหลัก/ผลลัพธ์]**
5. ผู้ใช้หรือ reviewer เห็น **[ผลลัพธ์/สถานะ]** และดำเนินการต่อได้

**Acceptance criteria ขั้นต่ำ:**

- ผู้ใช้ทำ workflow ให้จบได้โดยไม่ต้องพึ่งพาช่องทางนอกระบบ
- ข้อมูลที่บังคับกรอก กฎตรวจสอบ และข้อความผิดพลาดถูกกำหนดชัดเจน
- ผู้ใช้เห็นผลลัพธ์และสถานะล่าสุดของรายการได้
- ระบบบันทึกเหตุการณ์ที่จำเป็นต่อการติดตามข้อผิดพลาดและ audit

### 7.3 การจัดการข้อมูล

- ระบุ entity หลักของระบบ เช่น `User`, `Role`, `[PrimaryEntity]`, `[WorkflowItem]`
- กำหนดเจ้าของข้อมูล อายุข้อมูล สถานะวงจรชีวิต และกติกาการลบ/เก็บรักษา
- ระบุว่าข้อมูลใดเป็นข้อมูลส่วนบุคคล ข้อมูลลับ หรือข้อมูลสาธารณะ
- กำหนดการค้นหา กรอง เรียงลำดับ และ export เฉพาะเมื่อจำเป็นต่อ workflow ที่พิสูจน์แล้ว

### 7.4 การแจ้งเตือน (พิจารณาตาม use case)

- แจ้งเตือนในผลิตภัณฑ์หรือผ่าน email เมื่อมีเหตุการณ์สำคัญ เช่น การมอบหมาย การอนุมัติ หรือข้อผิดพลาด
- ผู้ใช้จัดการความต้องการรับแจ้งเตือนได้ตามนโยบาย
- ทุก notification ต้องมีเหตุผล ผู้รับ ช่องทาง และเงื่อนไขการส่งที่ระบุได้

### 7.5 การดูแลระบบ

- หน้าจอหรือเครื่องมือให้ admin จัดการผู้ใช้ role และการตั้งค่าจำเป็น
- บันทึกเหตุการณ์สำคัญและมีวิธีค้นหาสำหรับ support
- มีสถานะสุขภาพของบริการหรือวิธีตรวจสอบเหตุขัดข้องตามระดับบริการที่ตกลงกัน

## 8. User stories ตั้งต้น

| ลำดับ | User story | Priority | สถานะ |
| --- | --- | --- | --- |
| US-01 | ในฐานะผู้ใช้ ฉันต้องการเข้าสู่ระบบ เพื่อเข้าถึงข้อมูลและงานที่ได้รับอนุญาต | Must | ต้องยืนยันวิธี auth |
| US-02 | ในฐานะผู้ใช้ ฉันต้องการทำงานหลักของ Teoycodex จนเสร็จ เพื่อให้ได้ผลลัพธ์ตามเป้าหมาย | Must | ต้องระบุ workflow |
| US-03 | ในฐานะผู้ใช้ ฉันต้องการดูสถานะและผลลัพธ์ของรายการของฉัน เพื่อวางแผนการดำเนินการต่อ | Must | ต้องระบุ entity |
| US-04 | ในฐานะ reviewer ฉันต้องการตรวจสอบและดำเนินการกับรายการในขอบเขตของฉัน เพื่อควบคุมคุณภาพหรืออนุมัติงาน | Should | ต้องยืนยันว่ามี approval |
| US-05 | ในฐานะ admin ฉันต้องการกำหนด role `Admin` หรือ `User` ให้ผู้ใช้ เพื่อควบคุมการเข้าถึงอย่างเหมาะสม | Must | ยืนยันแล้ว |
| US-06 | ในฐานะ admin ฉันต้องการดูประวัติการเปลี่ยนแปลงสิทธิ์ เพื่อสืบค้นเหตุการณ์สำคัญ | Should | ต้องยืนยัน compliance |
| US-07 | ในฐานะผู้ใช้ ฉันต้องการได้รับการแจ้งเตือนเมื่อมีงานที่ต้องทำ เพื่อไม่พลาดการดำเนินการ | Could | ต้องยืนยันช่องทาง |

## 9. Functional requirements

| รหัส | Requirement | Priority | Acceptance criteria |
| --- | --- | --- | --- |
| FR-01 | ระบบต้องยืนยันตัวตนผู้ใช้ก่อนเข้าถึงข้อมูลที่ไม่ใช่สาธารณะ | Must | คำขอที่ไม่มี session/token ถูกปฏิเสธตามนโยบาย |
| FR-02 | ระบบต้องตรวจสอบสิทธิ์ตาม role ทุกการกระทำ | Must | `Admin` เข้าถึงได้ทุกหน้า; `User` ถูกจำกัดไว้เฉพาะหน้าทั่วไป ทั้ง UI และ API |
| FR-03 | ระบบต้องรองรับ workflow หลักที่ระบุได้ครบตั้งแต่เริ่มจนเห็นผลลัพธ์ | Must | ผู้ใช้เป้าหมายทำ scenario ที่ตกลงได้สำเร็จ |
| FR-04 | ระบบต้องตรวจสอบความถูกต้องของข้อมูลเข้าและแจ้งข้อผิดพลาดที่แก้ไขได้ | Must | ข้อมูลไม่ครบ/ไม่ถูกต้องไม่ถูกบันทึกโดยเงียบ ๆ |
| FR-05 | ระบบต้องแสดงสถานะและข้อมูลที่เกี่ยวข้องกับผู้ใช้ตามสิทธิ์ | Must | สถานะสะท้อนข้อมูลล่าสุดและไม่เปิดเผยข้อมูลเกินสิทธิ์ |
| FR-06 | Admin ต้องจัดการผู้ใช้และกำหนด role `Admin` หรือ `User` ได้ | Must | การเปลี่ยนแปลงมีผลและตรวจสอบย้อนกลับได้ |
| FR-07 | ระบบต้องสร้าง audit log สำหรับเหตุการณ์ความปลอดภัยและข้อมูลสำคัญ | Should | บันทึกมี actor เวลา action เป้าหมาย และผลลัพธ์ |
| FR-08 | ระบบต้องส่ง notification สำหรับเหตุการณ์ที่กำหนด | Could | ส่งเพียงครั้งเดียวต่อเหตุการณ์ตามเงื่อนไขที่ตั้งไว้ |

## 10. Non-functional requirements

| หมวด | Requirement ตั้งต้น | เกณฑ์ที่ต้องตกลง |
| --- | --- | --- |
| UI/UX | อินเทอร์เฟซต้องมีโทนมินิมัล เน้นงานหลักและลดองค์ประกอบที่ไม่จำเป็น | หน้าจอโปร่ง อ่านง่าย ใช้สีอย่างจำกัด มีลำดับชั้นข้อมูลชัดเจน และ responsive |
| Security | ป้องกันการเข้าถึงโดยไม่ได้รับอนุญาต และปกป้องข้อมูลระหว่างส่ง/จัดเก็บ | วิธี auth, encryption, session policy, secret management |
| Privacy | เก็บและใช้ข้อมูลเท่าที่จำเป็นต่อวัตถุประสงค์ | ประเภท PII, consent, retention, deletion |
| Performance | งานหลักต้องตอบสนองได้เหมาะกับงานจริง | p95 latency, จำนวนผู้ใช้พร้อมกัน, ขนาดข้อมูล |
| Availability | บริการต้องพร้อมใช้งานตามระดับที่ธุรกิจต้องการ | SLA/SLO, maintenance window, recovery target |
| Reliability | การทำงานสำคัญต้องไม่สูญหายหรือเกิดข้อมูลซ้ำโดยไม่ควบคุม | idempotency, backup, restore, retry behavior |
| Accessibility | ผู้ใช้ต้องใช้ workflow หลักได้ด้วยเทคโนโลยีช่วยเหลือ | มาตรฐานเป้าหมาย เช่น WCAG 2.2 AA |
| Compatibility | ระบุอุปกรณ์และ browser ที่รองรับ | browser/version, mobile/responsive requirement |
| Observability | ทีมต้องตรวจพบและวิเคราะห์ข้อผิดพลาดได้ | logging, metrics, alerting, trace retention |
| Localization | ระบุภาษาและรูปแบบวันเวลา/เขตเวลาที่รองรับ | ภาษา UI, locale, timezone ownership |

## 11. Success metrics

จะกำหนดตัวเลขเป้าหมายหลังทราบ baseline และกลุ่มผู้ใช้ แต่ควรเลือก metric ที่ผูกกับคุณค่าผลิตภัณฑ์จริง

| เป้าหมาย | Metric | สูตร/นิยาม | ค่าเป้าหมาย |
| --- | --- | --- | --- |
| การเริ่มใช้งาน | Activation rate | ผู้ใช้ใหม่ที่ทำ workflow หลักครั้งแรก / ผู้ใช้ใหม่ทั้งหมด | TBC |
| ความสำเร็จของงาน | Task completion rate | จำนวน workflow สำเร็จ / จำนวน workflow ที่เริ่ม | TBC |
| ประสิทธิภาพ | Median time to complete | เวลามัธยฐานตั้งแต่เริ่มจน workflow สำเร็จ | TBC |
| การใช้งานต่อเนื่อง | Retention | ผู้ใช้ที่กลับมาในช่วงที่กำหนด / ผู้ใช้ cohort เดียวกัน | TBC |
| คุณภาพ | Error/failure rate | การทำงานหลักที่ล้มเหลว / การทำงานหลักทั้งหมด | TBC |
| การสนับสนุน | Support contact rate | จำนวน ticket / จำนวนผู้ใช้ active | TBC |
| ความปลอดภัย | Unauthorized access success | จำนวนการเข้าถึงสำเร็จโดยไม่มีสิทธิ์ | ต้องเป็น 0 |

## 12. ข้อมูลและการวิเคราะห์

### 12.1 เหตุการณ์ที่ควรติดตาม

- `sign_up_completed`
- `login_succeeded` และ `login_failed`
- `role_changed`
- `core_workflow_started`
- `core_workflow_completed`
- `core_workflow_failed`
- `primary_entity_created` / `updated` / `deleted`
- `permission_denied`
- `notification_sent`

### 12.2 หลักการข้อมูล

- หลีกเลี่ยงการบันทึกข้อมูลลับหรือข้อมูลส่วนบุคคลที่ไม่จำเป็นใน analytics/log
- ระบุ data owner และ retention period ของทุกข้อมูลสำคัญ
- แยก audit log ออกจาก product analytics ตามความจำเป็นด้านสิทธิ์และ compliance
- กำหนดวิธี export/delete ข้อมูลเมื่อกฎหมายหรือนโยบายกำหนด

## 13. ความเสี่ยงและแนวทางลดความเสี่ยง

| ความเสี่ยง | ผลกระทบ | แนวทางลดความเสี่ยง |
| --- | --- | --- |
| กำหนดปัญหาหรือ persona ไม่ชัด | สร้างฟีเจอร์ที่ไม่มีความต้องการจริง | สัมภาษณ์ผู้ใช้และยืนยัน workflow ก่อนพัฒนา |
| นิยาม role/permission ไม่ครบ | ข้อมูลรั่วหรือผู้ใช้ทำงานไม่ได้ | ทำ permission matrix และทดสอบ authorization ทุก endpoint |
| ขอบเขต MVP ขยายเร็ว | ส่งมอบช้าและประเมินคุณค่าไม่ได้ | จัดลำดับ Must/Should/Could และใช้ decision log |
| ไม่ทราบคุณภาพ/ปริมาณข้อมูล | ออกแบบ data model และ performance ผิด | สำรวจแหล่งข้อมูล ขนาด และวงจรชีวิตก่อนเลือกสถาปัตยกรรม |
| ข้อกำหนดกฎหมายไม่ชัด | ความเสี่ยงด้าน compliance | ระบุประเทศ/อุตสาหกรรมและให้ผู้รับผิดชอบตรวจสอบ |
| ไม่มี baseline ของ metric | วัดความสำเร็จไม่ได้ | เก็บ baseline ใน pilot และกำหนด target หลังรอบแรก |

## 14. ข้อพึ่งพาและสมมติฐาน

### 14.1 Dependencies ที่ต้องยืนยัน

- ผู้ให้บริการยืนยันตัวตน หรือระบบบัญชีผู้ใช้เดิม
- แหล่งข้อมูลหลักและผู้รับผิดชอบข้อมูล
- ระบบส่ง email/notification
- ระบบภายนอกที่ต้องเชื่อมต่อ
- นโยบายความปลอดภัย กฎหมาย และการอนุมัติ
- ทีมดูแลหลังเปิดใช้และช่องทาง support

### 14.2 Assumptions ที่ต้องทดสอบ

- ผู้ใช้ยินดีใช้ระบบใหม่เพื่อทำงานหลักแทนวิธีปัจจุบัน
- รูปแบบ role-based access control เพียงพอต่อการควบคุมสิทธิ์ของโดเมนนี้
- สามารถเลือก workflow หลักหนึ่งเส้นทางเพื่อทดสอบคุณค่าใน MVP ได้
- มี owner ที่ตัดสินใจเรื่องข้อมูล สิทธิ์ และลำดับความสำคัญได้ทันเวลา

## 15. คำถามที่ต้องตอบก่อนเข้าสู่การออกแบบ/พัฒนา

### Product และผู้ใช้

1. Teoycodex แก้ปัญหาอะไร และปัญหานั้นเกิดกับใครบ่อยเพียงใด?
2. ผู้ใช้หลัก ผู้ใช้รอง และผู้จ่ายเงิน/ผู้อนุมัติ เป็นคนหรือองค์กรกลุ่มใด?
3. งานหลักที่ผู้ใช้ต้องทำให้สำเร็จในครั้งแรกคืออะไร?
4. ผู้ใช้ใช้วิธีหรือเครื่องมือใดในปัจจุบัน และสิ่งใดทำให้ไม่พอใจ?
5. MVP ต้องเปิดให้ผู้ใช้กลุ่มใด เมื่อใด และผ่านช่องทางใด?

### Role และสิทธิ์

6. ข้อความ “role ที่ต้…” ใน requirement เดิมต้องการสื่อ role ใดบ้าง?
7. แต่ละ role ดู สร้าง แก้ไข ลบ อนุมัติ หรือ export ข้อมูลใดได้บ้าง?
8. สิทธิ์ขึ้นกับองค์กร ทีม โปรเจกต์ หรือเจ้าของข้อมูลหรือไม่?
9. ใครสามารถแต่งตั้ง admin และกระบวนการอนุมัติเป็นอย่างไร?
10. ต้องมีประวัติการเปลี่ยนสิทธิ์หรือ approval สำหรับการกระทำใดบ้าง?

### ข้อมูลและเทคนิค

11. Entity หลักของระบบคืออะไร มี field สถานะ และความสัมพันธ์อย่างไร?
12. มีข้อมูลส่วนบุคคล ข้อมูลการเงิน หรือข้อมูลอ่อนไหวหรือไม่?
13. ต้องเชื่อมต่อระบบเดิม ระบบชำระเงิน หรือบริการภายนอกใดบ้าง?
14. Web, mobile หรือทั้งสองอย่างคือช่องทางที่ต้องรองรับ?
15. ข้อกำหนด availability, scale, browser, ภาษา และเขตเวลาคืออะไร?

### ธุรกิจและการส่งมอบ

16. อะไรคือเกณฑ์อนุมัติ MVP และ metric ใดตัดสินว่าควรลงทุนต่อ?
17. มี deadline, งบประมาณ, ทีม, หรือข้อจำกัดด้านเทคโนโลยีใดบ้าง?
18. ใครเป็นผู้ตัดสินใจสุดท้ายเมื่อ requirement ขัดกัน?
19. ต้องมีการอบรม คู่มือ หรือการย้ายข้อมูลหรือไม่?
20. จะรับ feedback และจัดการ incident หลังเปิดใช้งานอย่างไร?

## 16. Roadmap การค้นหา requirement

| ระยะ | ผลลัพธ์ที่ต้องได้ | เกณฑ์ออกจากระยะ |
| --- | --- | --- |
| Discovery | Problem statement, personas, core workflow, role list | Owner ยืนยันปัญหา ผู้ใช้ และ workflow หลัก |
| Definition | User stories, permission matrix, data model, acceptance criteria | ทุก Must requirement มี owner และเกณฑ์ทดสอบ |
| Design | Information architecture, UX flows, API/data contract | ผู้มีส่วนได้ส่วนเสียอนุมัติ flows สำคัญ |
| Build MVP | ฟีเจอร์ Must พร้อม observability และ security controls | ผ่าน acceptance criteria และการทดสอบที่ตกลง |
| Pilot | ใช้งานกับกลุ่มจำกัด เก็บ baseline และ feedback | ตัดสินใจ iterate, scale หรือหยุดได้จากข้อมูล |
| Launch | เปิดใช้ตามแผน พร้อม support และ operational runbook | SLO, owner และช่องทาง incident พร้อมใช้งาน |

## 17. Definition of Ready สำหรับเริ่มพัฒนา

ทีมสามารถเริ่มพัฒนา feature ได้เมื่อมีครบดังนี้:

- ระบุ user, problem และ expected outcome ชัดเจน
- ระบุ role ที่เกี่ยวข้องและ permission matrix สำหรับ feature นั้น
- มี user story และ acceptance criteria ที่ทดสอบได้
- กำหนดข้อมูลเข้า/ออก กฎ validation และกรณีผิดพลาด
- ประเมินความเสี่ยงด้าน security, privacy และ integration แล้ว
- ระบุ analytics/audit events ที่ต้องเก็บ
- มี product owner ตัดสินใจและยอมรับลำดับความสำคัญ

## 18. Changelog

| เวอร์ชัน | วันที่ | การเปลี่ยนแปลง |
| --- | --- | --- |
| 0.2 | 2026-08-29 | ยืนยัน UI โทนมินิมัลและ RBAC 2 บทบาท: `Admin` เข้าถึงทุกหน้า, `User` เข้าถึงเฉพาะหน้าทั่วไป |
| 0.1 | 2026-08-29 | สร้างเอกสาร Discovery จากชื่อ Teoycodex และ requirement ที่กล่าวถึง role แบบไม่สมบูรณ์ |
